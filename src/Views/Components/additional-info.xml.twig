{% if additionalInfo|length > 0 %}
<Otros>
  {% for item in additionalInfo %}
  {% set node = item.type == 'text' ? 'OtroTexto' : 'OtroContenido' %}
  <{{ node }} {% if item.code %} codigo="{{ item.code }}"{% endif %}>{{ item.value }}</{{ node }}>
  {% endfor %}
</Otros>
{% endif %}
